<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/background" /> <!-- Фон карточки (белый) -->
    <stroke
        android:width="6dp"
        android:color="@color/grad1" /> <!-- Чёрная обводка толщиной 2dp -->
    <corners android:radius="16dp" /> <!-- Закругленные углы, совпадающие с cardCornerRadius -->
</shape>